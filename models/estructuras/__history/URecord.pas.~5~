 unit URecord;

interface

type

Direcciones = Record
    izquierda : Boolean;
    derecha : Boolean;
    superior : Boolean;
    inferior : Boolean;
 End;

 Direction = (izquierda,derecha, superior, inferior, null);

 dato = Record
    valor : word;
    direccion : Direction;
 End;
implementation

end.
